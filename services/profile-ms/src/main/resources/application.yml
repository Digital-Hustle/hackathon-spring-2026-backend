spring:
  application:
    name: profile-ms
  cloud:
    config:
      uri: http://localhost:5000
      fail-fast: true
  config:
    import: "configserver:"


minio:
  endpoint: http://localhost:9000
  access-key: admin
  secret-key: root1234
  bucket-name: profile-ms
