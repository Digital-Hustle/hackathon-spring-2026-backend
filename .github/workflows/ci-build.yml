name: Spring microservices monorepo CI (Develop)

on:
  push:
    branches:
      - develop
      - 'feature/**'
  pull_request:
    branches:
      - 'feature/**'

jobs:
  common-jobs:
    uses: ./.github/workflows/common-jobs.yml
