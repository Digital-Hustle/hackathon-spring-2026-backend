spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_user_db?currentSchema=auth_user
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: auth_user
