spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_db?currentSchema=event
    username: postgres
    password: root

  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: event